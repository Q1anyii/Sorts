# 前端静态站点：SPA 兜底 + /api 反代到网关容器
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Docker 内置 DNS：让 proxy_pass 变量每 30s 重新解析网关容器地址，
    # 避免 nginx 启动时缓存旧 IP 导致网关重启后 /api 全部 502
    resolver 127.0.0.11 valid=30s ipv6=off;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # 带 hash 的静态资源长缓存
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 开发期：入口页禁用缓存，js/css 每次重新校验，避免改了文件浏览器还跑旧版
    location = /index.html {
        add_header Cache-Control "no-store";
    }
    location ~* \.(js|css)$ {
        add_header Cache-Control "no-cache";
    }

    # SPA 路由兜底（history 模式）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 网关在 sorts-net 内以服务名可达（变量方式：每个请求动态解析，网关重启/换 IP 不失效）
    location /api/ {
        set $gw http://gateway:8080;
        proxy_pass $gw;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # SSE（/ai/chat 流式）必须关闭缓冲，否则前端只能一次性收到整段
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        chunked_transfer_encoding on;
    }
}
