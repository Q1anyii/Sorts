# Devlog · 2026-09-17 · 全局气泡按钮对标浅色胶囊（第二次优化）

## 需求
用户反馈上一版全局气泡按钮（深色渐变圆钮）「太土」，给出对标样式：**浅色柔和圆角矩形胶囊 + 对应色图标与文字**（如「▶ 开梭」浅绿胶囊、「✎」浅灰圆钮）。

## 实现
### 模板（index.html，3 处：主计划气泡 / 子计划内嵌行 / 独立子气泡）
- 按钮从纯图标改为**图标 + 短文字**：`⏸ 暂停` / `▶ 续梭` / `⏹ 落梭` / `✕ 取消` / `✓ 完成`

### CSS（style.css `.gtb-btn` 重写）
- 形状：正圆 → **胶囊**（height 26px、padding 0 11px、border-radius 999px、gap 4px、white-space nowrap）
- 配色：深色渐变 → **浅色柔和底 + 深色同系文字 + 同色描边**
  - 暂停：浅琥珀 `rgba(251,191,36,.18)` / 深琥珀字 `#B45309`
  - 续梭/落梭：浅绿 `rgba(16,185,129,.14~.2)` / 深绿字 `#047857`
  - 取消：浅红 `rgba(239,68,68,.1)` / 深红字 `#DC2626`
- 交互：hover 底色加深 + 上浮 1px；active 缩至 .95

## 验证（浏览器端到端）
- 造今日日程（id=955）→ start → 刷新 #/plans 读取 computed style：
  - `⏸ 暂停` 62×26、bg rgba(251,191,36,.18)、color #B45309、radius 999px ✓
  - `⏹ 落梭` 61×26、bg rgba(16,185,129,.2)、color #047857 ✓
  - `✕ 取消` 60×26、bg rgba(239,68,68,.1)、color #DC2626 ✓
- 截图确认：浅色胶囊 + 图标文字，与对标样式一致
- 清理：id=955 cancel + delete，API 确认 0

## 变更文件
- `frontend/vite-app/index.html`：3 处气泡按钮加文字
- `frontend/vite-app/src/legacy/style.css`：`.gtb-btn` 系列重写

## 备注
- 未提交前完成 git 提交（先提交不推送）。
